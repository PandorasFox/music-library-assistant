[package]
name = "mm-ui"
version = "0.1.0"
edition = "2021"
description = "Shared UI logic for MM backends (TUI, web)"

[dependencies]
mm-meta = { path = "../mm-meta" }
ratatui = { version = "0.29", default-features = false }
serde = { version = "1.0", features = ["derive"] }
unicode-width = "0.2"
