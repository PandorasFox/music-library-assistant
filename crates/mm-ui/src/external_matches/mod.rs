//! Shared external match logic for TUI and web clients.
//!
//! Contains the approval builder that generates tag operations from
//! MB cache data + selected releases, used identically by both clients.

pub mod approval;
